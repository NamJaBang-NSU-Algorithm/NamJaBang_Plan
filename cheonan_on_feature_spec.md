# 천안 온 — 기능 명세서 (Feature Spec)

> 대상: Figma "천안 온 - UI/UX 디자인" 파일의 Screens 페이지, 총 12개 화면.
> 화면 ID는 Figma 프레임명을 그대로 사용한다. "트리거"는 해당 액션 발생 시의 화면 전환/상태 변화를 의미한다.
> 이 문서에 없는 기능(관리자 페이지, 알림 설정, 리뷰 좋아요·신고, 커뮤니티 게시판)은 화면으로 만들지 않았으므로 범위에서 제외했다 — 8절 "추후 고도화" 참고.

---

## 01_홈

**진입 조건**: 앱 최초 진입 시 기본 화면 (로그인 여부와 무관하게 접근 가능)

**표시 데이터**
- 히어로 검색바 ("행사명, 장소, 키워드로 검색")
- 카테고리 바로가기 5종: 축제 / 전시 / 공연 / 체험 / 계절행사
- 이번주 인기 행사 카드 그리드 (썸네일, 카테고리 뱃지, 제목, 일시, 장소, 가격, 북마크 아이콘)

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| 검색바에 입력 후 검색 | `02_행사목록`으로 이동 (검색어 파라미터 전달) |
| 카테고리 타일 클릭 | 해당 카테고리로 필터링된 `02_행사목록`으로 이동 |
| 인기 행사 카드 클릭 | `03_행사상세`로 이동 (행사 ID 전달) |
| 카드의 하트 아이콘 클릭 | 북마크 등록/해제 토글 (비로그인 시 `04_로그인`으로 유도) |
| 헤더 "행사 목록" 클릭 | `02_행사목록`으로 이동 |
| 헤더 프로필/로그인 클릭 | 로그인 상태면 `05_마이페이지`, 비로그인이면 `04_로그인`으로 이동 |

**예외/빈 상태**: 노출할 인기 행사가 0건인 경우(운영 초기) "등록된 행사가 없어요" 형태의 안내가 필요하나 현재 디자인에는 미반영 — 구현 시 추가.

---

## 02_행사목록

**진입 조건**: 헤더 "행사 목록" 클릭, 또는 `01_홈`의 카테고리 타일/검색에서 진입 (카테고리·검색어 파라미터를 전달받을 수 있음)

**표시 데이터**
- 뷰 토글: 리스트 / 캘린더 / 지도 (현재 화면은 "리스트" 활성 상태)
- 검색바
- 필터 칩: 카테고리(전체·축제·전시·공연·체험·계절행사), 지역구(전체·동남구·서북구), 가격(전체·무료·유료)
- 결과 건수 텍스트
- 행사 카드 그리드 (4열)

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| 뷰 토글 "캘린더" 클릭 | `02b_행사목록_캘린더뷰`로 전환 |
| 뷰 토글 "지도" 클릭 | `02c_행사목록_지도뷰`로 전환 |
| 필터 칩 클릭 | On/Off 토글, 같은 화면 내에서 결과 재조회 |
| 검색바 입력 | 결과 재조회. 0건이면 `02d_행사목록_검색결과없음` 상태로 렌더 |
| 행사 카드 클릭 | `03_행사상세`로 이동 (행사 ID 전달) |
| 카드 하트 아이콘 클릭 | 북마크 등록/해제 토글 (화면 전환 없음) |

**예외/빈 상태**: 필터/검색 결과가 0건이면 `02d_행사목록_검색결과없음`으로 분기.

---

## 02b_행사목록_캘린더뷰

**진입 조건**: `02_행사목록`에서 뷰 토글 "캘린더" 클릭 시

**표시 데이터**
- 월 네비게이션 (이전/다음 달 화살표, "YYYY년 M월" 라벨)
- 요일 헤더 (일~토)
- 날짜 셀 5~6행 × 7열. 행사가 있는 날짜에는 행사명 칩 표시 (해당 월이 아닌 날짜는 흐리게 표시)

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| 이전/다음 달 화살표 클릭 | 표시 월 변경, 해당 월 데이터로 캘린더 갱신 |
| 날짜 셀의 행사 칩 클릭 | `03_행사상세`로 이동 (행사 ID 전달) |
| 뷰 토글 "리스트"/"지도" 클릭 | `02_행사목록` / `02c_행사목록_지도뷰`로 전환 |

**예외/빈 상태**: 하루에 표시 가능한 칩 개수를 초과하는 경우(예: 4건 이상) "+N" 형태의 축약 표시가 필요하나 현재 디자인에는 미반영 — 구현 시 추가.

---

## 02c_행사목록_지도뷰

**진입 조건**: `02_행사목록`에서 뷰 토글 "지도" 클릭 시

**표시 데이터**
- 좌측: 스크롤 가능한 행사 리스트 패널 (썸네일, 카테고리, 제목, 장소, 가격)
- 우측: 지도 — 행사별 가격/무료 핀 마커, 사용자의 현재 위치 마커

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| 좌측 리스트 행 클릭 | 지도의 해당 핀 하이라이트, 또는 `03_행사상세`로 이동 |
| 지도 핀 클릭 | `03_행사상세`로 이동 (행사 ID 전달) |
| 지도 확대/축소 | 지도 줌 레벨 변경 (화면 전환 없음) |
| 뷰 토글 "리스트"/"캘린더" 클릭 | `02_행사목록` / `02b_행사목록_캘린더뷰`로 전환 |

**예외/빈 상태**: 표시할 행사가 0건이면 지도만 노출되고 좌측 리스트에 빈 상태 안내가 필요하나 현재 디자인에는 미반영. 위치 권한이 거부된 경우 내 위치 마커 없이 지도만 표시.

---

## 02d_행사목록_검색결과없음

**진입 조건**: `02_행사목록`에서 검색/필터 결과가 0건일 때

**표시 데이터**: 검색창(입력값 유지), 적용된 필터 칩, "검색 결과가 없어요" 안내 문구 + 설명, "필터 초기화" 버튼

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| "필터 초기화" 버튼 클릭 | 모든 필터·검색어 초기화 후 `02_행사목록` 전체 목록 상태로 복귀 |
| 검색어/필터 변경 | 재조회 결과가 1건 이상이면 `02_행사목록`으로 자동 전환 |

**예외/빈 상태**: 해당 없음 (이 화면 자체가 빈 상태).

---

## 03_행사상세

**진입 조건**: 행사 카드/지도 핀/캘린더 칩 클릭 시 (행사 ID 필수)

**표시 데이터**
- 히어로 이미지, 카테고리 뱃지, 북마크 버튼(우상단 오버레이)
- 제목, 메타 정보(일시/장소/평균 평점·리뷰 개수)
- 행사 소개 본문
- 상세 정보 목록: 일시/장소/요금/주최/문의/홈페이지
- 미니맵 + "길찾기" 버튼
- 리뷰 요약(평균 별점, 리뷰 개수), 리뷰 작성 폼(별점 선택 + 텍스트 + 사진 첨부), 리뷰 목록

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| "행사 목록으로" 뒤로가기 클릭 | `02_행사목록`으로 복귀 |
| 북마크 버튼 클릭 | 북마크 등록/해제 토글 (비로그인 시 `04_로그인`으로 유도) |
| "길찾기" 버튼 클릭 | `06_길찾기`로 이동 (행사 ID 전달) |
| 별점 선택 + 내용 입력 후 "리뷰 등록" 클릭 | 리뷰 생성, 리뷰 목록·평균 평점에 즉시 반영 |
| 패널 스크롤 | 세로 스크롤로 하단 콘텐츠 탐색 |

**예외/빈 상태**: 등록된 리뷰가 0건이면 `03b_행사상세_리뷰없음` 상태로 렌더.

---

## 03b_행사상세_리뷰없음

**진입 조건**: 리뷰가 아직 없는 행사의 `03_행사상세` 진입 시 (동일 화면의 상태 분기이며 별도 라우트는 아님)

**표시 데이터**: `03_행사상세`와 동일하나, 리뷰 요약·리뷰 목록 대신 "아직 등록된 리뷰가 없어요 · 첫 리뷰를 남겨주세요" 안내와 리뷰 작성 폼만 노출

**사용자 액션 / 트리거**: `03_행사상세`와 동일. 첫 리뷰 등록 시 리뷰 1건 이상 상태(`03_행사상세`의 일반 상태)로 전환.

---

## 04_로그인

**진입 조건**: 비로그인 상태에서 북마크/리뷰 작성/마이페이지 등 인증이 필요한 액션을 시도했을 때, 또는 헤더의 로그인 버튼 클릭 시

**표시 데이터**: 로고(로고마크 + "천안 온"), 서비스 소개 한 줄, 카카오 로그인 버튼, 약관·개인정보처리방침 동의 안내 문구

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| "카카오로 3초 만에 시작하기" 버튼 클릭 | 카카오 OAuth 인가 → 로그인/최초 가입 성공 시 진입 전 시도했던 화면(또는 `01_홈`)으로 복귀 |

**예외/빈 상태**: 카카오 로그인 실패 시 에러 메시지 노출 후 `04_로그인`에 머무름.

> 이메일/비밀번호 로그인, 회원가입 폼은 별도로 두지 않는다 — 카카오 로그인만 지원.

---

## 05_마이페이지

**진입 조건**: 헤더 프로필 아바타 클릭 시 (로그인 필수, 비로그인 시 `04_로그인`으로 유도)

**표시 데이터**: 프로필(아바타/이름/이메일), 탭(내 북마크 · 내가 쓴 리뷰) — 현재 화면은 "내 북마크" 탭 활성, 북마크한 행사 카드 그리드

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| "내가 쓴 리뷰" 탭 클릭 | `05b_마이페이지_내가쓴리뷰`로 전환 |
| 북마크 카드 클릭 | `03_행사상세`로 이동 |
| 카드 하트 아이콘 클릭 | 북마크 해제 (목록에서 즉시 제거) |
| 헤더 프로필 → 로그아웃 | 로그아웃 처리 후 `01_홈`으로 이동 |

**예외/빈 상태**: 북마크한 행사가 0건이면 `05c_마이페이지_북마크없음`으로 분기.

---

## 05b_마이페이지_내가쓴리뷰

**진입 조건**: `05_마이페이지`에서 "내가 쓴 리뷰" 탭 클릭 시

**표시 데이터**: 프로필/탭은 `05_마이페이지`와 동일, 내가 작성한 리뷰 리스트 (행사 썸네일, 제목, 별점, 리뷰 내용, 작성일, 삭제 아이콘)

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| "내 북마크" 탭 클릭 | `05_마이페이지`로 전환 |
| 리뷰 항목의 행사 썸네일/제목 클릭 | `03_행사상세`로 이동 |
| 삭제(X) 아이콘 클릭 | 확인 후 리뷰 삭제, 목록에서 즉시 제거 |

**예외/빈 상태**: 작성한 리뷰가 0건일 경우 `05c_마이페이지_북마크없음`과 동일한 패턴의 빈 상태 안내가 필요하나 현재 디자인에는 별도로 반영되어 있지 않음 — 구현 시 추가.

---

## 05c_마이페이지_북마크없음

**진입 조건**: `05_마이페이지` 진입 시 북마크한 행사가 0건일 때

**표시 데이터**: 프로필/탭은 `05_마이페이지`와 동일, "아직 찜한 행사가 없어요" 안내 + 설명 문구 + "행사 둘러보기" 버튼

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| "행사 둘러보기" 버튼 클릭 | `01_홈`(또는 `02_행사목록`)으로 이동 |

---

## 06_길찾기

**진입 조건**: `03_행사상세`에서 "길찾기" 버튼 클릭 시 (행사 ID 필수, 브라우저 위치 정보 권한 필요)

**표시 데이터**
- "행사 상세로 돌아가기" 링크, 행사명 + 주소
- 이동수단 탭: 대중교통(기본) / 자동차 / 도보
- 지도: 출발지(현재 위치) · 도착지(행사 장소) 마커
- 경로 요약: 총 소요시간, 이동 거리, 도착 예정 시각
- 단계별 경로 안내 리스트 (번호 매김)

**사용자 액션 / 트리거**
| 액션 | 트리거 |
|---|---|
| 이동수단 탭 전환 | 해당 수단 기준으로 경로/소요시간 재조회 및 갱신 |
| "행사 상세로 돌아가기" 클릭 | `03_행사상세`로 복귀 |

**예외/빈 상태**: 위치 정보 접근이 거부된 경우의 대체 안내, 해당 이동수단으로 경로를 찾을 수 없는 경우의 안내가 필요하나 현재 디자인에는 미반영 — 구현 시 추가.

---

## 공통 컴포넌트

Figma "Components" 페이지 기준, 모든 화면에서 재사용되는 요소:

| 컴포넌트 | 설명 |
|---|---|
| 헤더 | 로고마크+워드마크, 홈/행사목록/마이페이지 네비게이션, 검색·북마크 아이콘, 프로필 아바타(로그인 시) 또는 로그인 버튼(비로그인 시) — `State=LoggedIn` / `State=LoggedOut` 두 변형 |
| 푸터 | 로고, 서비스 한 줄 설명, 저작권 텍스트 |
| 이벤트 카드 | 썸네일, 카테고리 뱃지, 제목, 일시, 장소, 가격, 북마크 버튼 — `01_홈`/`02_행사목록`/`05_마이페이지`에서 공통 사용 |
| 버튼 | Style(Primary/Secondary/Outline) × Size(Default/Small) 변형 |
| 필터 칩 | State(Default/Selected) 변형 |
| 북마크 버튼 | Selected(true/false) 변형, 원형 아이콘 버튼 |
| 아이콘 세트 | search, heart(-filled), star(-filled/-empty), calendar, map-pin, chevron 등 24×24 벡터 아이콘 |
| 로고마크 | 코럴 배지 + 화이트 스파크 모티프 ("logo/mark") |

> 하단 고정 네비게이션 바는 이번 화면 세트에 포함하지 않는다 — 데스크톱 웹 기준 설계이므로 상단 헤더 내비게이션으로 대체.

---

## 8. 추후 고도화 시 고려 (이번 화면 세트에 미포함)
- 알림 설정 / 알림 발송 (D-day 알림 등)
- 리뷰 좋아요·댓글·신고
- 커뮤니티 게시판(자유/질문/정보 게시글·댓글)
- 관리자 페이지 (행사 등록/수정/삭제, 리뷰 신고 관리)
- 구글 캘린더 내보내기(ics)
- 추천 시스템
